@model List<Enfermo> 

<h1>Eliminar Enfermo</h1>

<form method="post">

    <label>Inscripcion</label>
    <input type="text" name="inscripcion" class="form-control"/>
    <br/>

    <button type="submit" class="btn btn-secondary">Eliminar Enfermo</button>
</form>


<table class="table table-active">
        <thead class="table table-danger">
            <tr>
                <th>Inscripcion</th>
                <th>Apellido</th>
                <th>Direccion</th>
                <th>Fecha_nac</th>
                <th>NSS</th>
            </tr>
        </thead>
        <tbody>

            @if (Model != null)
            {
                @foreach (Enfermo enf in Model)
                {

                    <tr>
                        <td>@enf.Inscripcion</td>
                        <td>@enf.Apellido</td>
                        <td>@enf.Direccion</td>
                        <td>@enf.Fecha_nac</td>
                        <td>@enf.NSS</td>
                    </tr>
                }
            }

            </tbody>
    </table>

